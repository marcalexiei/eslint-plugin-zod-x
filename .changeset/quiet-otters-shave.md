---
'@eslint-zod/utils': minor
---

feat: add `no-native-enum` and `no-promise-schema` rule builders

`buildNoNativeEnumCreate(scope)` and `buildNoPromiseSchemaCreate(scope)`, one per `rule-builders/` subpath.
